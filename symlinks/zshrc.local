#!/usr/bin/env zsh

# zsh configuration
DEFAULT_USER='yenbekbay'

# Synchronize base and local dotfiles
alias syncdotfiles="RCRC=${HOME}/.rcrc.local rcup -v"

# Stream movies to TV
stream() {
  webtorrent $1 --out ~/Downloads --dlna
}
stream-file() {
  dlnacast $1
}

edit() {
  z $1
  code .
}
